using System;
using System.Collections;
using System.Collections.Generic;
using System.Text;

namespace DataStructures
{
    public class RemoveDuplicates
    {

        public int data;
        public RemoveDuplicates preivous;


        public void deleteDuplicates(RemoveDuplicates duplicates)
        {
            Hashtable table = new Hashtable();
            RemoveDuplicates previous = null;

            while (duplicates != null)
            {

            }

        }
    }
}
